# dotfiles

Portable macOS terminal + desktop setup.

## Layout
- `config/` versioned configs to be symlinked into `~/.config`
- `local/` machine-specific overrides (ignored)

## Bootstrap
```sh
bash ~/dotfiles/bootstrap.sh
```
